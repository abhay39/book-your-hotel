/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['th.bing.com',"travelji.com"],
    },
    babel: {
      presets: ['next/babel'],
    },
  };
  
  module.exports = nextConfig;
  